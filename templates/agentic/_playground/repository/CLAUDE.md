# relay — the router

Read this first. It routes; it does not restate.

| Doing this                       | Read first               |
| -------------------------------- | ------------------------ |
| changing how a limit is computed | `docs/architecture.md`   |
| starting work that takes phases  | `docs/_plans/README.md`  |
| anything irreversible            | ask a person — the perimeter will stop a force-push anyway |

The roster is `.claude/agents/`. Only `lead` spawns; every other role is a leaf.
