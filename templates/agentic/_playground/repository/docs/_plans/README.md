# Plans

A plan is written for work that takes more than one sitting, and deleted — archived — when
the work lands. Each declares `**Status:**` and, once started, `**Branch:**`. Each phase
ends with the command that proves it done.
