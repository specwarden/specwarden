# harbor-infra

The deployment of the harbor services: a compose stack behind nginx, deployed by
`scripts/deploy.sh` and backed up nightly by `scripts/backup.sh`.

When something is on fire, start at `docs/runbooks/rollback.md`.
