# @specwarden/template-ops

## 0.1.0

### Minor Changes

- The first public release. Infrastructure — env pairing, proxy upstreams, shell scoping, runbooks — credential scan and documentation paths always on.

  For a repository whose product is the operation of something else.
