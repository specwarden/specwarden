# tally

Checkout and refunds for a small shop. Behaviour is specified first, in OpenSpec, and code
follows: what the system SHALL do lives under `openspec/specs/`, and a change in flight
lives under `openspec/changes/` until it is archived.

The code is in `src/checkout.ts` and `src/refunds.ts`.
