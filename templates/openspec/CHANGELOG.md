# @specwarden/template-openspec

## 0.1.0

### Minor Changes

- The first public release. A repository specified with OpenSpec — the spec source wired, credential scan, doc paths, lint and tests.

  The requirement source declared, so `sync-invariants` has both halves on day one.
