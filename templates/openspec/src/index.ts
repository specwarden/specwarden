/**
 * A starting tree for a repository specified with OpenSpec.
 *
 * OpenSpec owns the specification; this wires the SEAM, so the requirements it holds can
 * be reconciled against the invariants deposited in this repository's own documents.
 */
export { openspecTemplate } from './openspec/openspec.template';
