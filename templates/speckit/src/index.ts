/**
 * A starting tree for a repository specified with Spec Kit.
 *
 * Spec Kit owns `specs/<feature>/`; this wires the SEAM, so its requirements can be
 * reconciled against the invariants deposited in this repository's own documents.
 */
export { speckitTemplate } from './speckit/speckit.template';
