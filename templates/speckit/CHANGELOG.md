# @specwarden/template-speckit

## 0.1.0

### Minor Changes

- The first public release. A repository specified with Spec Kit — the spec source wired, credential scan, doc paths, lint and tests.

  The same as the OpenSpec template, against the other tool.
