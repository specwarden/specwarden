# doorman

Passwordless sign-in. A feature starts as a specification under `specs/`, written with
Spec Kit, and the principles every feature answers to are in
`.specify/memory/constitution.md`.

The link issuer is `src/magic-link.ts`; its behaviour is pinned by
`test/magic-link.test.ts`.
