/**
 * A starting tree for a NestJS backend.
 *
 * The plugin carries the stack's conventions; this template's job is to wire it with
 * the two facts no engine can guess — where the modules live and which package is the
 * ORM — and to add what a backend needs beside them.
 */
export { nestjsTemplate as nestjs } from './nestjs/nestjs.template';
