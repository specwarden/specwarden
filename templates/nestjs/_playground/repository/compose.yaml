services:
  api:
    build: .
    env_file: .env
    ports:
      - '3000:3000'
    depends_on:
      - db
  db:
    image: postgres:17
    environment:
      POSTGRES_DB: ledger
      POSTGRES_PASSWORD: ${DATABASE_PASSWORD}
