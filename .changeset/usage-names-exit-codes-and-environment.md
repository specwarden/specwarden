---
'specwarden': patch
---

`specwarden --help` lists the exit codes, every environment variable the engine reads (`CI`, `GITHUB_ACTIONS`, `SPECWARDEN_BASE`, `SPECWARDEN_ALL`, `SPECWARDEN_SKIP`, `SPECWARDEN_SHELL`), and `plan status` / `plan archive` with their arguments. The guide gains the same, a section on running in CI, and the plan conventions.
